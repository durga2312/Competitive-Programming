#include <bits/stdc++.h>
#define ll long long int
using namespace std;
int main()
{
	ios::sync_with_stdio(false);
	cin.tie(0);
	ll r;
	cin>>r;
	double pi;
	pi=atan(1.0)*(4.0);
	double ans;
	ans=(2.0)*(pi)*(r);
	cout<<ans<<"\n";
	return 0;
}
