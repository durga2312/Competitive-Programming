#include <bits/stdc++.h>
#define ll long long int
using namespace std;
int main()
{
	ios::sync_with_stdio(false);
	cin.tie(0);
		ll a;
		cin>>a;
		ll p;
		ll q;
		ll r;
		p=a;
		q=a*a;
		r=a*a*a;
		cout<<p+q+r<<"\n";
	return 0;
}
