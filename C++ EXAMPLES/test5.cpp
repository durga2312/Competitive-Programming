#include <bits/stdc++.h>
#define ll long long int
using namespace std;
int main()
{
	ios::sync_with_stdio(false);
	cin.tie(O);
	cin>>n;
	vector<ll> a;
	a.resize(n);
//	ll a[100];
	ll i;
	for(i=0;i<n;i++)
	{
		cin>>a[i];
	}
	return 0;
}
